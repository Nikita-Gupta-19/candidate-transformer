# Candidate Data Transformer

> **A deterministic candidate data transformation pipeline that merges structured and unstructured candidate information into a provenance-aware canonical profile with configurable runtime projection.**

This project was developed as part of the Eightfold AI Backend/Data Engineer internship assignment and demonstrates a deterministic pipeline for transforming heterogeneous candidate data into a unified canonical profile.

---

## 1. System Architecture

The pipeline processes heterogeneous data sources linearly through modular stages, standardizing inputs and resolving conflicts deterministically.

```text
       [ Recruiter CSV ]            [ Resume (PDF/DOCX/TXT) ]
               │                                │
               └─────────► Ingest & Extract ◄───┘
                             (Parsers)
                                 │
                        ┌────────┴────────┐
                        ▼                 ▼
                     Phones             Skills           Dates
                    (E.164)          (Synonyms)        (YYYY-MM)
                        ▲                 ▲                 ▲
                        └────────┬────────┘                 │
                                 ▼                          │
                             Normalize ◄────────────────────┘
                                 │
                                 ▼
                      Provenance & Confidence
                                 │
                                 ▼
                     Identity Matching & Merge
                    (Graph-based email/phone)
                                 │
                                 ▼
                    Conflict Resolution Policy
                    (Highest conf. -> CSV > Resume)
                                 │
                                 ▼
                            Schema Validation
                               (Pydantic)
                                 │
                                 ▼
                       Runtime Projection
                  (Path-based Configuration)
                                 │
                                 ▼
                        [ Final Output JSON ]
```

---

## 2. Directory Structure

```text
candidate-transformer/
├── main.py                 # CLI entry point & orchestrator
├── requirements.txt         # Project dependencies
├── config/
│   └── custom.json         # Custom output projection schema definition
├── inputs/
│   ├── candidate.csv       # Sample structured recruiter export
│   └── resume.txt          # Sample unstructured resume text
├── output.json             # Canonical output matching the default schema
├── custom_output.json      # Dynamic output generated by the custom projection
├── src/
│   ├── models.py           # Pydantic internal canonical schemas
│   ├── parsers/
│   │   ├── csv_parser.py   # Extract fields from structured recruiter CSV
│   │   └── resume_parser.py# Regex & heuristic text parsing (PDF/DOCX/TXT support)
│   ├── normalizers/
│   │   ├── phone.py        # Strict E.164 phone standardizer
│   │   ├── skills.py       # Synonyms standardizer
│   │   └── dates.py        # Date standardizer (maps to YYYY-MM)
│   ├── merger/
│   │   └── merger.py       # Graph identity grouping & conflict resolution
│   ├── projection/
│   │   └── projector.py    # Path-based runtime schema projection
│   └── validators/
│       └── validators.py   # Post-merge & post-projection schema validator
└── tests/
    ├── test_conflict_resolution.py
    ├── test_phone_normalization.py
    ├── test_projection_config.py
    └── test_skill_synonyms.py
```

---

## 3. Data Sources & Extensibility

### Implemented Sources
* **Structured**: **Recruiter CSV Export** (ingests tabular rows containing emails, phone numbers, skills, current company, and titles).
* **Unstructured**: **Resume Files** (automatically extracts raw text from PDF, DOCX, and TXT files using `pdfplumber` and `python-docx`, parsing emails, phone numbers, skills, and names with Regex and heuristics).

### Architecture Extensibility
The parser layer is fully decoupled from the normalization and merge pipeline. Additional sources—such as **LinkedIn Profiles (JSON/Scraped)**, **GitHub API Profiles**, or **Recruiter Notes (freeform text)**—can be integrated simply by writing a new parser that outputs the standard `CanonicalProfile` class. The downstream merge, validation, and projection pipeline requires no modifications.

---

## 4. Pipeline Execution Details

### A. Normalization Rules
* **Phone Numbers**: Normalizes strings to international E.164 format (e.g., `(415) 555-2671` -> `+14155552671`) via E.164 normalization.
* **Skills**: Maps spelling variations and synonyms (e.g., "JS", "Java Script" -> "JavaScript") using an extensible synonym lookup dictionary.
* **Dates**: Converts various date strings to standard `YYYY-MM` format.

### B. Grouping & Identity Merging
* Candidates are grouped into single profiles using **identity matching on normalized email and phone keys**.
* A graph of candidates is dynamically constructed where edges link records sharing an email or phone. Connected components of the graph are then merged.

### C. Conflict Resolution Policy
When merging scalar attributes (e.g., `full_name`) with conflicting values, the pipeline deterministically resolves the conflict using this sequence:
1. **Highest Confidence Score Wins**: The value with the higher calculated confidence is selected.
2. **Source Priority Tie-Breaker**: Structured (CSV) > Unstructured (Resume).
3. **Lexicographical Tie-Breaker**: If confidence and sources are identical, the alphabetically first string value is chosen, guaranteeing identical output across runs.

The priority model is extensible; additional source types (e.g., Recruiter Notes or LinkedIn) can be assigned source-specific priorities without modifying the merge algorithm.

### D. Confidence & Provenance Model
* **Base Confidences**:
  * Recruiter CSV = `0.95`
  * Resume = `0.75` (except name heuristic, which is `0.65`)
* **Multi-Source Boost**: If independent sources confirm the same list value (e.g., a skill), the confidence is boosted by `+0.05` per source, up to a maximum cap of `0.99` (avoiding hardcoded `1.0` certainty).
* **Provenance**: Every scalar value and list item tracks the exact `source`, extraction `method`, and calculated `confidence`.

---

## 5. Runtime Projection Layer

Different downstream applications require different schema configurations. The projection layer uses runtime JSON files to filter, rename, and reshape the canonical data dynamically via path-based configuration mapping.

### Example Config:
```json
{
  "fields": [
    { "path": "contact_email", "from": "emails[0].value", "required": true },
    { "path": "primary_phone", "from": "phones[0].value" },
    { "path": "top_skills", "from": "skills[].name" }
  ],
  "include_confidence": false,
  "on_missing": "null"
}
```
* **Default mapping**: If no configuration is supplied, the pipeline outputs the full, provenance-aware canonical schema.
* **Custom mapping**: When `--config` is provided, fields are mapped from path-based expressions (e.g., `emails[0].value` becomes `contact_email` in the output, and list operations like `skills[].name` extract only the skill names).

---

## 6. Getting Started

### Prerequisites
* Python 3.9+

### Setup
```bash
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

### Running the Pipeline
```bash
# Run default pipeline (outputs full canonical schema with provenance)
python main.py --csv inputs/candidate.csv --resume inputs/resume.txt

# Run pipeline with a custom JSON config projection
python main.py --csv inputs/candidate.csv --resume inputs/resume.txt --config config/custom.json --output custom_output.json
```

### Running Tests
To execute all unit tests verifying formatting, merging, conflict resolution, and projection:
```bash
PYTHONPATH=. pytest tests/
```

---

## 7. Sample Outputs

### Standard Output (`output.json`)
```json
[
  {
    "candidate_id": "e69cbdf7...",
    "full_name": "Nikita Gupta",
    "emails": [
      "nikitagpt06@gmail.com"
    ],
    "phones": [
      "+919818134486"
    ],
    "location": null,
    "links": null,
    "headline": null,
    "years_experience": null,
    "skills": [
      {
        "name": "Python",
        "confidence": 0.95,
        "sources": ["csv"]
      },
      {
        "name": "JavaScript",
        "confidence": 0.75,
        "sources": ["resume:resume.txt"]
      }
    ],
    "experience": [
      {
        "company": "Google",
        "title": "Software Engineer",
        "start": null,
        "end": null,
        "summary": null
      }
    ],
    "education": [],
    "overall_confidence": 0.9056,
    "provenance": [
      {
        "field": "full_name",
        "source": "csv",
        "method": "csv_column"
      },
      {
        "field": "experience[0].company",
        "source": "csv",
        "method": "csv_column"
      },
      {
        "field": "experience[0].title",
        "source": "csv",
        "method": "csv_column"
      }
    ]
  }
]
```

### Custom Projected Output (`custom_output.json`)
```json
[
  {
    "contact_email": "nikitagpt06@gmail.com",
    "primary_phone": "+919818134486",
    "top_skills": [
      "Python",
      "Machine Learning",
      "Node.js",
      "JavaScript"
    ]
  }
]
```
